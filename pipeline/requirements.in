# Direct dependencies for the pipeline image (human-edited source of truth).
# Bump deliberately, then recompile the hashed lockfile:
#   uv pip compile requirements.in --generate-hashes --python-version 3.12 -o requirements.txt
# Keep pandas/pyarrow versions in sync with backend/requirements.in.
pandas==3.0.3
openpyxl==3.1.5
requests==2.34.2
pyarrow==24.0.0
