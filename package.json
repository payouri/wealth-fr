{
  "name": "wealth-fr",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@11.1.2",
  "description": "Root dev orchestration: run the FastAPI backend and the Vite frontend together. App code lives in backend/ and frontend/.",
  "scripts": {
    "bootstrap": "python3 -m venv backend/.venv && backend/.venv/bin/pip install --upgrade pip && backend/.venv/bin/pip install -r backend/requirements-dev.txt && pnpm --dir frontend install",
    "dev": "concurrently -k -n api,web -c blue,magenta \"backend/.venv/bin/uvicorn app.main:app --app-dir backend --reload --reload-dir backend/app --host 127.0.0.1 --port 8000\" \"pnpm --dir frontend dev\"",
    "api": "backend/.venv/bin/uvicorn app.main:app --app-dir backend --reload --reload-dir backend/app --host 127.0.0.1 --port 8000",
    "web": "pnpm --dir frontend dev"
  },
  "devDependencies": {
    "concurrently": "9.2.1"
  }
}
