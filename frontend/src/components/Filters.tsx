// STUB: first-level filters. `unite` and `concept_patrimoine` (the Convention)
// must be exposed here as primary filters — never collapsed away. See CONTEXT.md.
export default function Filters() {
  return (
    <div className="rounded border bg-white p-4 text-sm text-gray-500">
      Filtres (source · indicateur · groupe · concept · unité · euros constants) — TODO
    </div>
  );
}
