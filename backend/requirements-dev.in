# Test/CI-only dependencies (human-edited source of truth). Not in the prod image.
# ruff + mypy are repo-wide: they also lint/type-check pipeline/ (see pyproject.toml).
# Bump deliberately, then recompile the hashed lockfile:
#   uv pip compile requirements-dev.in --generate-hashes --python-version 3.12 -o requirements-dev.txt
-r requirements.in
pytest
httpx
ruff
mypy
